<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="/css/output.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
</head>
<body class="bg-background min-h-screen flex flex-col">
    <!-- Navigation -->
    <nav class="bg-white shadow-sm border-b border-gray-200 sticky top-0 z-50">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between items-center h-16">
                <!-- Logo -->
                <div class="flex-shrink-0">
                    <a href="/" class="text-2xl font-bold text-primary">
                        🎵 CalmTunes
                    </a>
                </div>
                
                <!-- Navigation Links -->
                <div class="hidden md:block">
                    <div class="ml-10 flex items-baseline space-x-4">
                        <% if (user) { %>
                            <a href="/dashboard" class="nav-link">Dashboard</a>
                            <a href="/music" class="nav-link">Music</a>
                            <a href="/drawing" class="nav-link">Art Therapy</a>
                            <a href="/panic" class="nav-link">Panic Relief</a>
                            <a href="/mood-tracker" class="nav-link">Mood Tracker</a>
                            <a href="/therapists" class="nav-link">Therapists</a>
                            <a href="/logout" class="bg-red-500 hover:bg-red-600 text-white px-3 py-2 rounded-md text-sm font-medium transition-colors duration-200">
                                Logout
                            </a>
                        <% } else { %>
                            <a href="/" class="nav-link">Home</a>
                            <a href="/about" class="nav-link">About</a>
                            <a href="/login" class="btn-primary">Login</a>
                            <a href="/signup" class="btn-secondary">Sign Up</a>
                        <% } %>
                    </div>
                </div>
                
                <!-- Mobile menu button -->
                <div class="md:hidden">
                    <button id="mobile-menu-button" class="text-textMain hover:text-primary">
                        <i class="fas fa-bars text-xl"></i>
                    </button>
                </div>
            </div>
        </div>
        
        <!-- Mobile menu -->
        <div id="mobile-menu" class="hidden md:hidden bg-white border-t border-gray-200">
            <div class="px-2 pt-2 pb-3 space-y-1 sm:px-3">
                <% if (user) { %>
                    <a href="/dashboard" class="block px-3 py-2 text-textMain hover:text-primary">Dashboard</a>
                    <a href="/music" class="block px-3 py-2 text-textMain hover:text-primary">Music</a>
                    <a href="/drawing" class="block px-3 py-2 text-textMain hover:text-primary">Art Therapy</a>
                    <a href="/panic" class="block px-3 py-2 text-textMain hover:text-primary">Panic Relief</a>
                    <a href="/mood-tracker" class="block px-3 py-2 text-textMain hover:text-primary">Mood Tracker</a>
                    <a href="/therapists" class="block px-3 py-2 text-textMain hover:text-primary">Therapists</a>
                    <a href="/logout" class="block px-3 py-2 text-red-600 hover:text-red-800">Logout</a>
                <% } else { %>
                    <a href="/" class="block px-3 py-2 text-textMain hover:text-primary">Home</a>
                    <a href="/about" class="block px-3 py-2 text-textMain hover:text-primary">About</a>
                    <a href="/login" class="block px-3 py-2 text-textMain hover:text-primary">Login</a>
                    <a href="/signup" class="block px-3 py-2 text-textMain hover:text-primary">Sign Up</a>
                <% } %>
            </div>
        </div>
    </nav>

    <!-- Flash Messages -->
    <% if (success && success.length > 0) { %>
        <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 mx-4 mt-4 rounded">
            <%= success %>
        </div>
    <% } %>
    
    <% if (error && error.length > 0) { %>
        <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 mx-4 mt-4 rounded">
            <%= error %>
        </div>
    <% } %>

    <!-- Main Content -->
    <main class="flex-grow">
        <%- body -%>
    </main>

    <!-- Footer -->
    <footer class="bg-gray-50 border-t border-gray-200 mt-auto">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
            <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
                <div>
                    <h3 class="text-lg font-semibold text-primary mb-4">CalmTunes</h3>
                    <p class="text-gray-600 text-sm">Your mental health companion, providing tools and resources for emotional well-being.</p>
                </div>
                <div>
                    <h4 class="text-md font-semibold text-textMain mb-4">Quick Links</h4>
                    <ul class="space-y-2 text-sm">
                        <li><a href="/about" class="text-gray-600 hover:text-primary">About Us</a></li>
                        <li><a href="#" class="text-gray-600 hover:text-primary">Privacy Policy</a></li>
                        <li><a href="#" class="text-gray-600 hover:text-primary">Terms of Service</a></li>
                        <li><a href="#" class="text-gray-600 hover:text-primary">Contact</a></li>
                    </ul>
                </div>
                <div>
                    <h4 class="text-md font-semibold text-textMain mb-4">Support</h4>
                    <p class="text-gray-600 text-sm mb-2">Crisis Support: 988</p>
                    <p class="text-gray-600 text-sm">help@calmtunes.com</p>
                </div>
            </div>
            <div class="border-t border-gray-200 mt-8 pt-4 text-center">
                <p class="text-gray-500 text-sm">&copy; 2024 CalmTunes. All rights reserved.</p>
            </div>
        </div>
    </footer>

    <script src="/js/main.js"></script>
</body>
</html>